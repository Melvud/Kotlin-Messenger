rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /chats/{chatId}/images/{fileId} {
      allow read: if request.auth != null; // можно сузить до участников чата с проверкой Firestore via get
      allow write: if request.auth != null;
    }
  }
}